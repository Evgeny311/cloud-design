apiVersion: v1
kind: Service
metadata:
  name: inventory-app-service
  namespace: microservices
  labels:
    app: inventory-app
    project: cloud-design
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30001
    protocol: TCP
    name: http
  selector:
    app: inventory-app