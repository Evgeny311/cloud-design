apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-app
  namespace: microservices
  labels:
    app: inventory-app
    project: cloud-design
spec:
  replicas: 2
  selector:
    matchLabels:
      app: inventory-app
  template:
    metadata:
      labels:
        app: inventory-app
        version: v1
    spec:
      containers:
      - name: inventory-app
        image: 711893265334.dkr.ecr.eu-north-1.amazonaws.com/cloud-design/dev/inventory:v1.1.0
        imagePullPolicy: Always
        command: ["python", "-u", "server.py"]  # ← ДОБАВЛЕНО
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        
        # Environment variables
        envFrom:
        - configMapRef:
            name: inventory-app-config
        
        env:
        # Database credentials from secret
        - name: DB_HOST
          valueFrom:
            secretKeyRef:
              name: database-secret
              key: inventory-host
        - name: DB_USERNAME
          valueFrom:
            secretKeyRef:
              name: database-secret
              key: inventory-username
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: database-secret
              key: inventory-password
        - name: DB_NAME
          valueFrom:
            secretKeyRef:
              name: database-secret
              key: inventory-database
        
        # Resource limits (УЖЕ ХОРОШИЕ)
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        
        # Health checks - УДАЛЕНЫ (нет /health endpoint)
        # livenessProbe: ...
        # readinessProbe: ...
      
      imagePullSecrets:
      - name: ecr-secret
      
      securityContext:
        runAsNonRoot: false
        runAsUser: 1000